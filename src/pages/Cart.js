import React from "react";
import styled from 'styled-components';

const Cart = (props) => {
  
  return (
    <React.Fragment>
        <Title>카트</Title>
    </React.Fragment>
  )

}

export default Cart;

const Title = styled.h2`
  text-align: center;
`